# Roekish — staging build (noindex)

Static staging build for **render validation only**. Not indexed, not for public
distribution: every page carries `noindex,nofollow` and `robots.txt` is
`Disallow: /`. No lead collection here. The public, indexed site lives on
roekish.fr.
